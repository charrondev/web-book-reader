import styled from "@emotion/styled";

export const ContentContainer = styled.div({
    paddingLeft: 40,
    paddingRight: 40,
    "@media (max-width: 768px)": {
        paddingLeft: 16,
        paddingRight: 16,
    },
});
